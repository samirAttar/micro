spring:
 application:
  name: USER.SERVICE


eureka:
 client:
  registry-with-eureka: true
  fetch-registry: true
  service-url:
  default-zone: http://localhost:8761/eureka/
  instance:
  hostname: localhost 